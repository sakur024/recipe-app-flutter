/// Global constants for the Recipe App.
///
/// Only values that are already needed across multiple files belong here.
/// Do not add speculative constants for features that have not been built.
abstract final class AppConstants {
  /// Display name shown in the AppBar, splash, etc.
  static const String appName = 'Recipe App';

  /// Standard horizontal page padding.
  static const double pagePadding = 16.0;

  /// Default border radius used for cards, buttons, inputs.
  static const double borderRadius = 12.0;

  /// Larger border radius used for image containers and modals.
  static const double borderRadiusLarge = 16.0;
}
